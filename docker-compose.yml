name: ragpoisoner

services:
  ollama:
    image: ollama/ollama:latest
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:11434/api/tags"]
      interval: 15s
      timeout: 10s
      retries: 15
      start_period: 30s

  ragpoisoner:
    image: ragpoisoner:latest
    build:
      context: .
      dockerfile: docker/Dockerfile
    environment:
      - OLLAMA_HOST=http://ollama:11434
      - OLLAMA_MODEL=${OLLAMA_MODEL:-mistral}
      - CHROMA_DB_PATH=/data/chroma_db
      - EMBEDDING_MODEL=all-MiniLM-L6-v2
      - VERBOSE=1
    volumes:
      - ./docker/corpus:/app/corpus:ro
      - ./docker/results:/app/results
      - ./docker/chroma_data:/data/chroma_db
    depends_on:
      ollama:
        condition: service_healthy
    command: >
      full-scan
      --corpus-dir /app/corpus
      --query "What is the security policy?"
      --output-dir /app/results

volumes:
  ollama_data:
