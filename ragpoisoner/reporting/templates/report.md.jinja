# RAG Corpus Poisoning Scan Report

**Generated:** {{ generated_at }}
**Target query:** `{{ target_query }}`
**Overall risk:** {{ overall_risk }}

---

## Executive Summary

| Metric | Value |
|---|---|
| Poisoning attacks run | {{ inject_results | length }} |
| Successfully retrieved | **{{ inject_results | selectattr("retrieved_in_top_k") | list | length }}** |
| Injection tests run | {{ injection_results | length }} |
| Injections succeeded | **{{ injection_results | selectattr("injection_succeeded") | list | length }}** |

---

## Module 1: Corpus Poisoning Results

{% for r in inject_results %}
### `{{ r.payload_type }}` — {{ "VULNERABLE" if r.retrieved_in_top_k else "BLOCKED" }}

- **Severity:** `{{ r.severity }}`
- **Retrieved in top-k:** {{ r.retrieved_in_top_k }}
- **Output influenced:** {{ r.influenced_output }}
- **Retrieval score:** {{ "%.4f" | format(r.retrieval_score_after) }}

{% endfor %}

---

## Module 2: Instruction Injection Results

{% for r in injection_results %}
### `{{ r.test_name }}` — {{ "VULNERABLE" if r.injection_succeeded else "PROTECTED" }}

- **Description:** {{ r.description }}
- **Severity:** `{{ r.severity }}`
- **Details:** {{ r.details }}

{% endfor %}

{% if persistence_results %}
---

## Module 3: Persistence Analysis

{% for r in persistence_results %}
- **Influence rounds:** {{ r.influence_rounds }}
- **Survived re-index:** {{ r.survived_reindex }}
- **Survived sanitization:** {{ r.survived_sanitization }}
- **Semantic variants working:** {{ r.semantic_variant_count }}
{% endfor %}
{% endif %}

---

## Remediation Recommendations

1. Sanitize all documents before indexing
2. Implement output filtering for injection markers
3. Add retrieval auditing
4. Enforce document provenance
5. Re-run after every corpus update

---

_Generated by **ragpoisoner** — for authorized security testing only._
